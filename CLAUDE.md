# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Описание проекта

Это статический веб-сайт для визуализации RACI-матрицы полного цикла работы с клиентом для компании "Пчелошеринг Пасека Михаила Клецкого". Проект состоит из одного HTML-файла с встроенными стилями и автоматически деплоится на GitHub Pages.

## Структура проекта

- `index.html` - единственный HTML-файл, содержащий всю разметку и стили
  - Все CSS встроены в `<style>` блок внутри `<head>`
  - Нет внешних зависимостей или JavaScript
  - Статический контент на русском языке
- `.github/workflows/pages.yml` - конфигурация GitHub Actions для автодеплоя
- `README.md` - описание проекта на русском языке

## Деплой

**Автоматический деплой:** При пуше в ветку `main` GitHub Actions автоматически публикует сайт на GitHub Pages.

**Ручной деплой:** Можно запустить workflow вручную через GitHub UI (workflow_dispatch).

**Локальный просмотр:** Просто откройте файл `index.html` в браузере - никакая сборка или сервер не требуются.

## Контент и структура RACI-матрицы

Сайт отображает:
- **Легенду RACI:** И (Исполнитель), О (Ответственный), К (Консультант), Инф (Информируемый)
- **9 ролей в команде:** от Продакт-оунеров до Дизайнера
- **11 этапов процесса:** от обращения клиента до работы с возражениями
- **Цветовое кодирование:**
  - Зеленый (#4CAF50) - Исполнитель
  - Синий (#2196F3) - Ответственный
  - Оранжевый (#FF9800) - Консультант
  - Серый (#9E9E9E) - Информируемый

## Редактирование контента

При внесении изменений в матрицу:
1. Все изменения делаются напрямую в `index.html`
2. Обновите версию документа в футере (формат: ДД.ММ.ГГГГ)
3. Следите за соблюдением правил RACI: только один Ответственный (О) на каждый этап
4. Используйте существующие классы для ролей: `role-responsible`, `role-accountable`, `role-consulted`, `role-informed`

## Стилистика и дизайн

- **Адаптивность:** Сайт оптимизирован для мобильных устройств (@media queries на 768px)
- **Печать:** Специальные стили для печати (@media print)
- **Градиенты:** Используются по всему сайту для создания современного вида
- **Sticky заголовки:** Заголовки таблицы фиксируются при прокрутке
- **Эмоджи:** Используются в тексте (🐝, 📊, 💡 и т.д.)

## Git workflow

- **Основная ветка:** `main`
- **Коммиты:** Следуйте формату существующих коммитов (см. git log)
- **После изменений:** Изменения автоматически публикуются на GitHub Pages
